# =====================================================
# 🚀 AI SAAS BACKEND - ENVIRONMENT VARIABLES
# =====================================================
# Copy file này thành .env và điền thông tin của bạn
# =====================================================

# ──────────────────────────────
# 🌐 APP CONFIGURATION
# ──────────────────────────────
NODE_ENV=production
APP_URL=https://your-domain.com
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ──────────────────────────────
# 🗄️ DATABASE (BẮT BUỘC)
# ──────────────────────────────
# Lấy từ: Neon.tech, Supabase, hoặc Railway
# ⚠️ Nhớ dùng POOLER endpoint!
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require&connection_limit=1

# Example Neon.tech:
# DATABASE_URL=postgresql://neondb_owner:npg_xxxx@ep-abc-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ──────────────────────────────
# 🔐 AUTHENTICATION (BẮT BUỘC)
# ──────────────────────────────
# Tạo bằng: openssl rand -base64 32
AUTH_SECRET=REPLACE_WITH_32_CHARS_RANDOM_SECRET
AUTH_COOKIE_NAME=session

# Email verification
REQUIRE_EMAIL_VERIFICATION=false

# ──────────────────────────────
# 🤖 AI PROVIDERS (Ít nhất 1 cái)
# ──────────────────────────────

# OpenAI (Khuyến nghị)
# Lấy từ: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-REPLACE_WITH_YOUR_KEY

# Anthropic Claude (Tùy chọn)
# Lấy từ: https://console.anthropic.com/account/keys
# ANTHROPIC_API_KEY=sk-ant-REPLACE_WITH_YOUR_KEY

# Google Gemini (Tùy chọn - Free tier tốt!)
# Lấy từ: https://makersuite.google.com/app/apikey
# GOOGLE_API_KEY=AIza_REPLACE_WITH_YOUR_KEY

# Groq (Tùy chọn - Rất nhanh, free)
# Lấy từ: https://console.groq.com/keys
# GROQ_API_KEY=gsk_REPLACE_WITH_YOUR_KEY

# AI Configuration
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
AI_TIMEOUT_MS=45000

# ──────────────────────────────
# 🔴 REDIS - RATE LIMITING (BẮT BUỘC)
# ──────────────────────────────
# Lấy từ: https://console.upstash.com
# Free tier: 10,000 commands/day
UPSTASH_REDIS_REST_URL=https://REPLACE.upstash.io
UPSTASH_REDIS_REST_TOKEN=REPLACE_WITH_YOUR_TOKEN

# Rate limit settings
RATE_PM=60
MAX_HISTORY=20

# ──────────────────────────────
# 📧 EMAIL (Khuyến nghị)
# ──────────────────────────────

# Option 1: Gmail SMTP (Dễ nhất)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your.email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM="My SaaS <noreply@yourdomain.com>"

# Option 2: Resend (Khuyến nghị cho production)
# Lấy từ: https://resend.com/api-keys
# RESEND_API_KEY=re_REPLACE_WITH_YOUR_KEY

# ──────────────────────────────
# 💳 PAYMENT - PayOS (Tùy chọn)
# ──────────────────────────────
# Lấy từ: https://my.payos.vn/
# PAYOS_CLIENT_ID=your_client_id
# PAYOS_API_KEY=your_api_key
# PAYOS_CHECKSUM_KEY=your_checksum_key

# ──────────────────────────────
# 📦 STORAGE - File Uploads (Tùy chọn)
# ──────────────────────────────

# Option 1: Cloudflare R2 (Khuyến nghị - Free 10GB)
# Lấy từ: Cloudflare Dashboard → R2
# R2_ACCOUNT_ID=your_account_id
# R2_ACCESS_KEY_ID=your_access_key
# R2_SECRET_ACCESS_KEY=your_secret_key
# R2_BUCKET_NAME=my-saas-uploads
# R2_PUBLIC_URL=https://uploads.yourdomain.com

# Option 2: Azure Blob Storage
# AZURE_STORAGE_ACCOUNT_NAME=yourstorageaccount
# AZURE_STORAGE_ACCOUNT_KEY=your_storage_key
# AZURE_BLOB_CONTAINER_NAME=uploads

# ──────────────────────────────
# 📊 MONITORING (Tùy chọn)
# ──────────────────────────────

# Sentry - Error tracking
# Lấy từ: https://sentry.io (Free: 5K errors/month)
# SENTRY_DSN=https://xxxxx@o123456.ingest.sentry.io/123456

# ──────────────────────────────
# 📈 ANALYTICS (Tùy chọn)
# ──────────────────────────────

# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# PostHog (Free: 1M events/month)
# NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxx
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ──────────────────────────────
# 🛡️ CLOUDFLARE (Tùy chọn)
# ──────────────────────────────

# Turnstile CAPTCHA (Free, thay thế reCAPTCHA)
# CLOUDFLARE_TURNSTILE_SECRET=0x4xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY=0x4AAAxxxxxxxxxxxxxxxxxx

# ──────────────────────────────
# 🚫 DEVELOPMENT FLAGS (DEV ONLY)
# ──────────────────────────────
# ⚠️ PHẢI SET = 0 Ở PRODUCTION!
MOCK_AI=0
DEV_BYPASS_PAY=0
DEV_BYPASS_LIMIT=0
DEBUG_OPENAI=0

# ──────────────────────────────
# ✅ DEPLOYMENT CHECKLIST
# ──────────────────────────────
# [ ] DATABASE_URL configured với pooler
# [ ] AUTH_SECRET generated (32+ chars)
# [ ] Ít nhất 1 AI provider key
# [ ] UPSTASH_REDIS configured
# [ ] Email configured (nếu cần verification)
# [ ] All dev flags = 0
# [ ] Run: npm run env:verify
# ──────────────────────────────
